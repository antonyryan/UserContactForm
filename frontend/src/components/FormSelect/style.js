import { makeStyles } from '@mui/styles'

export default makeStyles({
  formControl: {
    minWidth: 120,
    width: '100%',
    marginBottom: '16px',
  },
  noMb: {
    marginBottom: '0 !important',
  },
})
